import 'package:flutter/material.dart';

/// WARNA & TEMA — premium dark + gold accent
class AppColors {
  static const Color bg = Color(0xFF0F1115); // grafit gelap
  static const Color card = Color(0xFF161A20);
  static const Color gold = Color(0xFFD4AF37);
  static const Color goldSoft = Color(0xFFE6C766);
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFB8C1CC);
}
